# Промпты и правила проекта ContextFlow — сводка одним документом

> Что агент держит в голове и по каким файлам сверяется. Всё в одном месте.

---

## 1. Рабочие правила (зафиксированы в сессии)

### Что и где править
- В демо правлю **только** `judge` / `task` / `tr` (+ словарь `EXP` по явной команде рецензии). Структуру сцен, имена узлов, вёрстку **не трогаю**.
- Ход — это **цель** («поздоровайся и назовись»), а не диктовка фразы. Диктовка запрещена.
- Судья обязан принимать **свободный** ответ по смыслу: порядок слов не важен, лишние слова игнорируются, точное совпадение с `best` НЕ требуется.
- Текстовые правки — только с сохранением **судейской семантики**: каждая ветка, которую принимал старый судья, принимается и новым.

### Механика, которую нельзя ломать (регресс-список)
1. «Не знаю» → проговаривается `best`, диалог идёт дальше.
2. Подсказка появляется **только после 2 непониманий** (`huh`).
3. Сцена заканчивается **только через прощание** (узел с `next: null`).
4. `best` никогда не показывается как «правильный ответ» до попытки.
5. Автопроверка голоса: копится лучший результат за попытку (`bestHeard`/`bestScore`).
6. Ввод текста: никакого сжатия/фиксации/прибивки поля; плавный подскролл только при реальном перекрытии клавиатурой.
7. В диалоге: вопрос и поле ввода рядом, компактный 🔊 у реплики, «Не знаю» — заметная кнопка.

### Диалоги и курс
- Диалоги делаем **от боли релоканта**, а не по порядку списка. Лексика — по программе, содержание диктует боль первых 90 дней.
- Сцены боли ставятся **на тематическое место в начале этапа**; метки «ур.» пересчитывает скрипт. Аэропорт не трогать.
- Первые 3 диалога демо-5 **уже в курсе** (ур. 3/6/9) — не портировать заново.
- Демо-репо (`contextflow-demos`) — **только диалоговые демки по 3 сцены**.

### Процесс и качество
- **Все проверки идут через логи.** Валидация = `qa/flow-test.js` (логика) + `qa/ui-log.js` (UI-прогон всего проекта). Прогон **перед каждым деплоем**.
- Любое падение = строка в отчёте (`✗`), любой обрыв/недоигранный уровень = `⚠`.
- «Готово» говорю **только после деплоя**.
- **Секреты — не в чат** (токены, ключи).
- Повторяющийся класс ошибок лечу **системно**, а не точечно (позитивный ключ раньше отрицания).
- **Не начинаю большую работу по двусмысленной команде** — сначала уточняю / доказываю текущее состояние.
- Гайд релоканта — **после MVP**. Редизайн — **без конфига не двигаться**.

### Чек «источника истины» для баг-репорта
Баг-репорт без **всех трёх** пунктов — невалиден:
1. точный путь / URL;
2. SHA файла;
3. кусок `judge` из раннер-лога.

---

## 2. Файлы-источники в воркспейсе

| Файл | Что это |
|---|---|
| `ПРАВИЛА-ТЕСТИРОВАНИЯ.md` | Главный регламент: что тестируем, чем, когда имеем право сказать «готово»; чек источника истины + раздел прогонки через логи |
| `ОЧЕРЕДЬ-ПО-БОЛИ.md` | Очередь сцен от самой сильной боли релоканта к меньшей (что делаем следующим) |
| `ГАЙД-РЕЛОКАНТА-БЭКЛОГ.md` | Бэклок гайда по 5 странам (отложен до после MVP) |
| `qa/flow-test.js` | 623 автотеста логики диалогов |
| `qa/ui-log.js` | Headless-прогон всего проекта: каждый уровень «кликается» и «печатается», каждый шаг пишется в лог сам |
| `qa/ui-log-report.md` | Сводный отчёт последнего прогона |
| `qa/ui-log-trace.log` | Полный машинный трейс действий (клик/ввод/рендер) |

---

## 3. Рецензионные промпты от оценщика (в `uploads/`) — все закрыты

| Промпт | К чему относится | Статус |
|---|---|---|
| `промпт-демо5 (1).md` | демо-5, первый круг | ✅ закрыт |
| `промпт-демо5-2.md` | демо-5, второй круг | ✅ закрыт |
| `промпт-демо5-3.md` | демо-5, третий круг | ✅ закрыт |
| `промпт-демо5-4.md` | демо-5, четвёртый круг | ✅ закрыт |
| `промпт-демо5-N5.md` | демо-5, регресс-блок N5 | ✅ закрыт |
| `промпт-демо5-R5.md` | демо-5, регресс-блок R5 | ✅ закрыт |
| `промпт-демо5-баги (1).md` | демо-5, точечные баги | ✅ закрыт |
| `промпт-демо6.md` | демо-6, первый круг | ✅ закрыт |
| `промпт-демо6-2.md` | демо-6, второй круг | ✅ закрыт |
| `промпт-демо9-10.md` | демо-9/10, первый круг | ✅ закрыт |
| `промпт-демо9-10-2.md` | демо-9/10, второй круг | ✅ закрыт |
| `промпт-партия34.md` | партия 3–4, первый круг | ✅ закрыт |
| `промпт-партия34-2.md` | партия 3–4, второй круг | ✅ закрыт |

Вспомогательные материалы владельца в `uploads/`: `КАК-ПРОВЕРЯТЬ.md`, `выжимка-демо5.md`.

---

## 4. Чего ещё нет

- **Промпт для нового кодэкса-оценщика** ещё не написан. Пойдёт вместе с **демо-7**, когда оно будет собрано.

---

## 5. Оркестрация: роли, Git-процесс и критерии (введено владельцем 16.09.2026)

### Роли
- **Воркер (Главный Разработчик)** — пишет чистый код строго по задачам, не ломает имеющуюся логику и автотесты.
- **Тестировщик** — не просто гоняет автотесты, а намеренно ломает код (краш-режим).
- **Судья** — смысловая валидация свободных ответов.

### Правила Git и защита деплоя
- **Запрет прямой работы в `main`.** Любая фича/багфикс — STRICTLY в ветке `feature/название-задачи`.
- **Единая точка деплоя:** публикация на GitHub Pages (v34+) только из `main` (скрипт `обновить-сайт.sh`).
- **Merge-критерий (Definition of Done):** слияние `feature` в `main` разрешено ТОЛЬКО после машинного лог-отчёта Тестировщика со строгими показателями **`0 ✗` и `0 ⚠`** (прогон `qa/ui-log.js` + зелёный `qa/flow-test.js`).
- Локальная рабочая ветка переведена на имя `main` (бывший `master`).

### Технический критерий Судьи (уточнение семантики)
- «Свободный ответ по смыслу» = валидация **обязательных ключевых смысловых сущностей (keywords/intent)**, а не ослабление регулярок.
- Судья **не принимает** ответы, не относящиеся к контексту сцены. Не зажимать до точного совпадения с `best`.

### Регламент Воркера
1. Каждая задача — в отдельной ветке `feature/…`; никаких пушей напрямую в `main`.
2. Изоляция изменений: правлю только `judge` / `task` / `tr`; структуру сцен, имена узлов, вёрстку и сторонние модули — без явного указания не трогать.
3. Защита от регрессии: запрещено менять имена ключей и структуру объектов в `lessons.js` (ломает 623 автотеста).
4. Ответ на QA-отчёт: найденные `✗`/`⚠` исправляю **в этой же ветке** новым коммитом.
5. «Готово» = все автотесты зелёные + изменения запушены.

### Режим Тестировщика (краш-тестирование)
Принцип недоверия. Обязательные провокации:
1. **Грязный ввод:** спецсимволы `!@#$%^&*()`, абракадабра, пустой текст, кириллица, строки > 1000 символов.
2. **Стресс состояний:** сбои сети, порча ключей `localStorage`, блокировка медиа-потоков (Vosk/микрофон).
3. **Новые негативные сценарии** в `qa/ui-log.js` при каждой новой фиче.

**Формат QA-отчёта:** ответ «всё работает» НЕвалиден. Отчёт = перечень симулированных крайних случаев + машинные логи упавших тестов (если найдены).

---

*Обновлено: 2026-09-16 · сайт на v33, тесты 623/0 и 160/0, прогон всего проекта 290 уровней / 0 падений.*

---

## 6. Архитектурный стандарт (роль Lead Architect, введено 17.09.2026)

- **Обратная совместимость — красная линия.** Никогда не ломать существующую функциональность и тестовое окружение (623/146/160, ui-log с краш-секцией, идентичность демок). Любые изменения — изолированные (feature-ветка + QA-гейты перед мержем).
- **Чистая архитектура.** Бизнес-логика (Judge Engine) / UI / данные (Storage, State) раздельно; код пишется так, чтобы масштабироваться и переноситься в модули/фреймворки в будущем. ES-модули и WebComponents — целевой стиль для нового кода.
- **Надёжность.** Все асинхронные операции — с try/catch и fallback-состояниями; event listeners и таймеры корректно удаляются при смене экранов/сцен (без утечек памяти); минимум перерисовок DOM, никаких тяжёлых синхронных операций в основном потоке.
- **Формат выдачи.** Краткий план (1–3 пункта) + полный продакшен-код без заглушек (`// TODO` запрещены).

---

## 7. Режим «Нулевой автопилот» (жёсткие рамки владельца, 17.09.2026)

- **Один пункт за ход.** Выполняется исключительно план (`MVP-ПЛАН.md`), берётся ТОЛЬКО пункт, указанный в текущей задаче владельца. Связки (1.1+1.2+1.3) запрещены. Самопостановка отменена.
- **Роль: Lead Full-Stack Engineer & Software Architect (Vanilla JS).**
  - Гибкость судейства: оценка смысла/намерений через маркеры и синонимы, без жёстких строк; масштабируемо от A1 до C2.
  - Сохранность тестов: весь harness и контракты данных неприкосновенны; изменения — изолированно (Strangler Fig).
  - Качество: без библиотек, try/catch на асинхронность, чистка listeners/таймеров, никаких `// TODO`.
- Жду указание пункта — выполняю только его, до зелёных гейтов, и останавливаюсь.

---

## 8. Процесс передачи задач (жёсткая схема владельца, 17.09.2026)

Каждое сообщение владельца = отдельная задача. Никаких связок.

1. **Изоляция:** перед работой — `git checkout main` и новая ветка `feature/название-задачи` от свежего main. В `main` не кидается НИЧЕГО.
2. **Точечные правки:** только разрешённые узлы (`judge` / `task` / `tr`); чужие файлы и структуру `lessons.js` не трогать без явного указания.
3. **Фиксация и пуш:** коммит и `git push origin feature/название-задачи`. Только ветка.
4. **Передача эстафеты:** сообщить владельцу имя ветки, точный URL и SHA коммита — для передачи Тестировщику.
5. **Исправление по логам:** баги от Тестировщика чинятся в этой же ветке, новый пуш, снова на проверку. Без споров.
6. **Финал:** «Готово» — только после отчёта Тестировщика с 0 ✗ и 0 ⚠. Мерж в main — не моя операция.
