# 🤖 Мост между агентами — как включить (10 минут)

## Зачем
Чтобы ты не таскал файлы вкладками. Два Arena-агента (внедрятор и QA-сверщик)
общаются через GitHub-репозиторий. Твоя роль — одна фраза каждому за цикл.

## Шаг 1. Токен (делаешь ты, 2 минуты)
1. github.com → твой аватар → **Settings**
2. Внизу слева: **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. **Generate new token** → имя `agent-bridge` → галочка **`repo`** → Generate
4. Скопируй строку вида `ghp_...` — она покажется один раз

## Шаг 2. Дай токен внедрятору (в этом чате)
Одним сообщением: `вот токен: ghp_... создай приватный репо и настрой мост`
Внедрятор сам:
- создаст приватный репозиторий `contextflow-обмен`,
- настроит push в ветку `workspace`,
- положит `.github/workflows/qa.yml` → QA-робот заведётся сам,
- запушет свежие демо и код.

> ⚠️ Публичный репо с сайтом НЕ трогаем: GitHub Pages работает только с ним.
> Токен потом можно отозвать — всё уже настроено.

## Шаг 3. Дай доступ сверщику
Скопируй сверщику (в его чат):

```
Ты — QA-сверщик диалогов ContextFlow. Работаешь с ПРИВАТНЫМ репозиторием
<имя репо от внедрятора>, ветка workspace. Токен: <тот же ghp_...>
Правила:
1. Перед проверкой: git pull origin workspace.
2. Свежие демо — в корне (демо-6 (1).html, дальше демо-7...).
3. Механику НЕ гоняй руками: читай QA-АВТО.md (его пишет робот).
4. Ищи только смысловые дефекты: отрицания, странные ответы, петли, мусор.
5. Отчёт пиши в ОТЧЁТ-QA.md: узел → ввод → что сейчас → что должно быть.
6. git push origin workspace. Если чисто — пиши «ЧИСТО».
```

## Дальше — твой минимум на цикл
- внедрятору: **«дальше»**
- сверщику: **«проверь»**

## Честно про «фоном»
Сами агенты в фоне не живут — они просыпаются от твоего сообщения.
Но: механические проверки делает GitHub-робот сам, а агент внедряет партиями
по одной команде. Больше 2 фраз за цикл от тебя не нужно.
